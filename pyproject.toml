[build-system]
requires = ["hatchling>=1.18,<1.27"]
build-backend = "hatchling.build"

[project]
name = "projectmem"
version = "0.2.0"
description = "AI coding memory — a local-first memory and judgment layer for AI coding agents that warns before repeating failed fixes."
authors = [{ name = "Ripon Chandra Malo" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = [
  # phrase-based: single generic words ("ai", "memory") cannot rank on PyPI
  "ai-coding-memory", "agent-memory", "ai-agent-memory", "agentic-memory",
  "memory-for-ai-coding-agents", "persistent-memory", "llm-memory",
  "mcp", "mcp-server", "model-context-protocol",
  "claude", "claude-code", "cursor", "codex", "coding-agent",
  "local-first", "developer-tools", "pre-commit", "ai-memory",
]
dependencies = [
  "typer>=0.12",
  "mcp>=0.1.0,<2",
  "tiktoken>=0.7,<1",
  "watchdog>=4.0",
]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Documentation",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff>=0.4",
]

[project.urls]
Homepage = "https://projectmem.dev"
Repository = "https://github.com/riponcm/projectmem"
Documentation = "https://projectmem.dev/guide"
Changelog = "https://projectmem.dev/changelog"
Issues = "https://github.com/riponcm/projectmem/issues"
Paper = "https://arxiv.org/abs/2606.12329"

[project.scripts]
projectmem = "projectmem.cli:main"
pjm = "projectmem.cli:main"
pjm-mcp = "projectmem.mcp_server:main"
pjm-mcp-global = "projectmem.mcp_global_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/projectmem"]

[tool.hatch.build.targets.sdist]
# marketing assets are linked from GitHub in the README; no need to ship them to pip users
exclude = ["brand/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint.per-file-ignores]
# Typer declares CLI options as evaluated defaults. The remaining entries are
# explicit best-effort/error-boundary handlers whose comments define the
# fallback contract; linting every new file remains strict.
"src/projectmem/cli.py" = ["B008"]
"src/projectmem/commands/brief.py" = ["BLE001"]
"src/projectmem/commands/dashboard.py" = ["BLE001", "S110"]
"src/projectmem/commands/export.py" = ["BLE001", "S110"]
"src/projectmem/commands/init.py" = ["BLE001", "S110"]
"src/projectmem/commands/precheck.py" = ["BLE001", "S110"]
"src/projectmem/commands/watch.py" = ["BLE001"]
"src/projectmem/global_memory.py" = ["BLE001", "S110"]
"src/projectmem/mcp_server.py" = ["BLE001"]
"src/projectmem/storage.py" = ["BLE001", "S110"]
"src/projectmem/summary.py" = ["BLE001"]
